{
  "name": "busnoti",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "dev:api": "pnpm --filter @busnoti/api dev",
    "dev:web": "pnpm --filter @busnoti/web dev",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prisma": "^7.3.0"
  }
}
